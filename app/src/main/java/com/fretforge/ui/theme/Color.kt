package com.fretforge.ui.theme

import androidx.compose.ui.graphics.Color

// Primary — Warm Amber Gold (guitar strings / warmth)
val AmberGold        = Color(0xFFE8A838)
val AmberGoldLight   = Color(0xFFFFC85A)
val AmberGoldDark    = Color(0xFFB07A1A)
val AmberGoldDim     = Color(0x33E8A838)

// Secondary — Electric Blue (amp glow)
val ElectricBlue     = Color(0xFF5C8EFF)
val ElectricBlueLight= Color(0xFF8AB0FF)
val ElectricBlueDark = Color(0xFF2D5FCC)
val ElectricBlueDim  = Color(0x335C8EFF)

// Backgrounds / Surfaces
val DeepBlack        = Color(0xFF0D0D0F)   // near-black background
val DarkCharcoal     = Color(0xFF18181C)   // surface
val DarkCard         = Color(0xFF1E2028)   // surface variant / cards
val DarkCardElevated = Color(0xFF24262E)   // elevated card
val DarkOutline      = Color(0xFF3A3A44)   // outlines / dividers

// Text / On colors
val OnDarkPrimary    = Color(0xFF0D0D0F)   // on amber (dark for contrast)
val OnDarkSecondary  = Color(0xFF0D0D0F)   // on blue (dark for contrast)
val OnDarkBackground = Color(0xFFEEEEF2)   // primary text
val OnDarkSurface    = Color(0xFFEEEEF2)
val TextSecondary    = Color(0xFF9999AA)   // subdued text
val TextTertiary     = Color(0xFF666678)   // very muted

// Status / Semantic
val ErrorRed         = Color(0xFFFF5C5C)
val ErrorRedDim      = Color(0x33FF5C5C)
val SuccessGreen     = Color(0xFF4CAF82)

// Beat states
val BeatActive       = AmberGold
val BeatAccent       = ElectricBlue
val BeatMuted        = Color(0xFF2E2E3A)
